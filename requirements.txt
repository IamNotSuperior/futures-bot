pandas
numpy
vectorbt
matplotlib
pyarrow
python-dotenv
databento

# Bots
discord.py
pyyaml

# /submit sends strategy submissions to the Claude API and writes the
# generated code into strategies/generated/. See bots/generate.py.
anthropic

# Desk bot webhook receiver. uvicorn serves the FastAPI app that TradingView
# alerts POST 1-minute bars to; httpx backs fastapi.testclient.TestClient, which
# tests/test_desk_feed.py uses to drive the endpoint without opening a socket.
fastapi
uvicorn
httpx

# Testing
pytest

# vectorbt 1.1.0 builds its plotly themes using the `scattermapbox` trace type,
# which plotly 6+ renamed to `scattermap`. Unpinned, pip installs plotly 7.x and
# `import vectorbt` fails at import time.
plotly<6

# ProjectX Gateway (TopstepX) bar client. Already present transitively via
# databento; declared here because data/topstepx.py imports it directly.
requests
